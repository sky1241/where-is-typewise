openapi: 3.0.0
info:
  title: probe
  version: "1.0"
  description:
    $ref: 'file:///etc/hostname'
paths:
  /x:
    get:
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: 'file:///etc/hostname'
